[project]
name = "ambient-agent"
version = "0.1.0"
description = "A self-hosted personal AI workspace with durable runs, graph data, and sandboxed widgets."
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = { file = "LICENSE" }
authors = [
    { name = "Shiyao Zhang" },
]
keywords = ["agent", "ai", "llm", "self-hosted", "workspace"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]
dependencies = [
    "agent-client-protocol>=0.11.0",
    "fastapi>=0.139.0",
    "uvicorn>=0.51.0",
    "sqlmodel>=0.0.39",
    "websockets>=16.0",
    "pydantic==2.13.4",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.2",
    "httpx>=0.28.1",
    "litellm==1.92.0",
    "neo4j>=6.0,<7",
    "orjson>=3.10.0",
    "pyyaml>=6.0.3",
]

[project.urls]
Homepage = "https://github.com/BillShiyaoZhang/ambient-agent"
Documentation = "https://github.com/BillShiyaoZhang/ambient-agent/tree/main/docs"
Repository = "https://github.com/BillShiyaoZhang/ambient-agent.git"
Issues = "https://github.com/BillShiyaoZhang/ambient-agent/issues"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.9.0",
]

[tool.ruff]
# Target Python version
target-version = "py311"
line-length = 120

# Enable standard rules
lint.select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "B",    # flake8-bugbear
    "C4",   # flake8-comprehensions
    "UP",   # pyupgrade
    "RUF",  # ruff-specific rules
]
lint.ignore = [
    "E501",   # line too long (handled by formatter)
    "B904",   # raise ... from err
    "RUF006", # asyncio.create_task reference
    "RUF012", # mutable class attribute defaults
    "RUF013", # implicit Optional
    "E741",   # ambiguous variable name
    "B007",   # unused loop variable
    "F841",   # unused local variable assignment
    "RUF059", # unused unpacked variables
    "B008",   # function call in argument defaults (FastAPI Depends)
    "E402",   # module level import not at top of file
    "RUF001", # string contains ambiguous double-width punctuation
    "E721",   # type comparison check (instead of isinstance)
    "I001",   # import sorting
    "W291",   # trailing whitespace
    "W293",   # blank line contains whitespace
]

# Exclude common directories
exclude = [
    ".git",
    ".pytest_cache",
    ".ruff_cache",
    ".venv",
    "venv",
    "__pycache__",
    "workspace",
]

[tool.ruff.lint.isort]
known-first-party = ["backend"]
